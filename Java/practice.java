class practice{
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
// commands: ( must compile then run or direct java filename.java)
// for compilation:- javac filename.java
// for execution(run):- java classname
/*system.out.println("hello world") -> not manditory to write arguments inside
here in println , it prints line by line


system.out.print("hello world ")  -> manditory to write arguments inside
 sam line with spaces wil be printed
two types of print statements 
*/

// typecasting : widening narrowing
        //         widening : converting smaller data type to larger data type.
        //         narrowing : converting larger data type to smaller data type.    
        // widening is done automatically but narrowing is not done automatically we have to do it manually.
        // widening is done by compiler but narrowing is done by programmer.

    // skipped operators and conditionals and loops
    
    // input:
    //         Scanner sc = new Scanner(System.in);
    //         int a = sc.nextInt();    
    //         String str = sc.nextLine();
    //         float f = sc.nextFloat();
    //         double d = sc.nextDouble();
    // byte: byte b = sc.nextByte();
    // short: short s = sc.nextShort();
    // long: long l = sc.nextLong();
    // char: char c = sc.next().charAt(0); // to take single character input.
    // boolean: boolean bool = sc.nextBoolean(); // to take boolean input.
    //         sc.close(); // to close the scanner object.
    //         we have to import java.util.Scanner; // to use scanner class.